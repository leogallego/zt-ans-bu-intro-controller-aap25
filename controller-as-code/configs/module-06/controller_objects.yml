---
# Module 06: Creating a Job Template
# Creates: Install Apache job template

controller_templates_module06:
  - name: Install Apache
    organization: Default
    state: present
    inventory: Lab-Inventory
    become_enabled: true
    playbook: apache.yml
    project: Apache playbooks
    credential: lab-credentials
